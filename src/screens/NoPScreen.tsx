import React from 'react';
import { ScrollView, Text, View } from 'react-native';

const NoPScreen = () => {
  return (
    <ScrollView>
      <View>
        <Text>Aviso de privacidad</Text>
      </View>
    </ScrollView>
  );
};

export default NoPScreen;
